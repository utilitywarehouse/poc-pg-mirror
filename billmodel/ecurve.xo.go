// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Ecurve represents a row from 'equinox.ecurve'.
type Ecurve struct {
	Ecfuel     sql.NullString `json:"ecfuel"`      // ecfuel
	Ecprofile  sql.NullString `json:"ecprofile"`   // ecprofile
	Ecregister sql.NullString `json:"ecregister"`  // ecregister
	EquinoxLrn int64          `json:"equinox_lrn"` // equinox_lrn
	EquinoxSec sql.NullInt64  `json:"equinox_sec"` // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Ecurve exists in the database.
func (e *Ecurve) Exists() bool {
	return e._exists
}

// Deleted provides information if the Ecurve has been deleted from the database.
func (e *Ecurve) Deleted() bool {
	return e._deleted
}

// Insert inserts the Ecurve to the database.
func (e *Ecurve) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if e._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ecurve (` +
		`ecfuel, ecprofile, ecregister, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxSec)
	err = db.QueryRow(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxSec).Scan(&e.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	e._exists = true

	return nil
}

// Update updates the Ecurve in the database.
func (e *Ecurve) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !e._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if e._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ecurve SET (` +
		`ecfuel, ecprofile, ecregister, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4` +
		`) WHERE equinox_lrn = $5`

	// run query
	XOLog(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxSec, e.EquinoxLrn)
	_, err = db.Exec(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxSec, e.EquinoxLrn)
	return err
}

// Save saves the Ecurve to the database.
func (e *Ecurve) Save(db XODB) error {
	if e.Exists() {
		return e.Update(db)
	}

	return e.Insert(db)
}

// Upsert performs an upsert for Ecurve.
//
// NOTE: PostgreSQL 9.5+ only
func (e *Ecurve) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if e._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ecurve (` +
		`ecfuel, ecprofile, ecregister, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`ecfuel, ecprofile, ecregister, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.ecfuel, EXCLUDED.ecprofile, EXCLUDED.ecregister, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxLrn, e.EquinoxSec)
	_, err = db.Exec(sqlstr, e.Ecfuel, e.Ecprofile, e.Ecregister, e.EquinoxLrn, e.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	e._exists = true

	return nil
}

// Delete deletes the Ecurve from the database.
func (e *Ecurve) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !e._exists {
		return nil
	}

	// if deleted, bail
	if e._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ecurve WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, e.EquinoxLrn)
	_, err = db.Exec(sqlstr, e.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	e._deleted = true

	return nil
}

// EcurveByEquinoxLrn retrieves a row from 'equinox.ecurve' as a Ecurve.
//
// Generated from index 'ecurve_pkey'.
func EcurveByEquinoxLrn(db XODB, equinoxLrn int64) (*Ecurve, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`ecfuel, ecprofile, ecregister, equinox_lrn, equinox_sec ` +
		`FROM equinox.ecurve ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	e := Ecurve{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&e.Ecfuel, &e.Ecprofile, &e.Ecregister, &e.EquinoxLrn, &e.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &e, nil
}
