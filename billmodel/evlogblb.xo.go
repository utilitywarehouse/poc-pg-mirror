// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// EvlogBlb represents a row from 'equinox.evlog_blb'.
type EvlogBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the EvlogBlb exists in the database.
func (eb *EvlogBlb) Exists() bool {
	return eb._exists
}

// Deleted provides information if the EvlogBlb has been deleted from the database.
func (eb *EvlogBlb) Deleted() bool {
	return eb._deleted
}

// Insert inserts the EvlogBlb to the database.
func (eb *EvlogBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if eb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.evlog_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, eb.BlbData, eb.BlbText)
	err = db.QueryRow(sqlstr, eb.BlbData, eb.BlbText).Scan(&eb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	eb._exists = true

	return nil
}

// Update updates the EvlogBlb in the database.
func (eb *EvlogBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !eb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if eb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.evlog_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, eb.BlbData, eb.BlbText, eb.BlbLrn)
	_, err = db.Exec(sqlstr, eb.BlbData, eb.BlbText, eb.BlbLrn)
	return err
}

// Save saves the EvlogBlb to the database.
func (eb *EvlogBlb) Save(db XODB) error {
	if eb.Exists() {
		return eb.Update(db)
	}

	return eb.Insert(db)
}

// Upsert performs an upsert for EvlogBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (eb *EvlogBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if eb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.evlog_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, eb.BlbLrn, eb.BlbData, eb.BlbText)
	_, err = db.Exec(sqlstr, eb.BlbLrn, eb.BlbData, eb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	eb._exists = true

	return nil
}

// Delete deletes the EvlogBlb from the database.
func (eb *EvlogBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !eb._exists {
		return nil
	}

	// if deleted, bail
	if eb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.evlog_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, eb.BlbLrn)
	_, err = db.Exec(sqlstr, eb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	eb._deleted = true

	return nil
}

// EvlogBlbByBlbLrn retrieves a row from 'equinox.evlog_blb' as a EvlogBlb.
//
// Generated from index 'evlog_blb_pkey'.
func EvlogBlbByBlbLrn(db XODB, blbLrn int64) (*EvlogBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.evlog_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	eb := EvlogBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&eb.BlbLrn, &eb.BlbData, &eb.BlbText)
	if err != nil {
		return nil, err
	}

	return &eb, nil
}
