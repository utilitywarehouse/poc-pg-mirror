// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"

	"github.com/lib/pq"
)

// Gmprompt represents a row from 'equinox.gmprompt'.
type Gmprompt struct {
	Gmpfrequency     sql.NullInt64 `json:"gmpfrequency"`     // gmpfrequency
	Gmplastactiond   pq.NullTime   `json:"gmplastactiond"`   // gmplastactiond
	Gmplastaction    sql.NullInt64 `json:"gmplastaction"`    // gmplastaction
	Gmpfreqsetbycust sql.NullInt64 `json:"gmpfreqsetbycust"` // gmpfreqsetbycust
	EquinoxPrn       sql.NullInt64 `json:"equinox_prn"`      // equinox_prn
	EquinoxLrn       int64         `json:"equinox_lrn"`      // equinox_lrn
	EquinoxSec       sql.NullInt64 `json:"equinox_sec"`      // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Gmprompt exists in the database.
func (g *Gmprompt) Exists() bool {
	return g._exists
}

// Deleted provides information if the Gmprompt has been deleted from the database.
func (g *Gmprompt) Deleted() bool {
	return g._deleted
}

// Insert inserts the Gmprompt to the database.
func (g *Gmprompt) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.gmprompt (` +
		`gmpfrequency, gmplastactiond, gmplastaction, gmpfreqsetbycust, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxSec)
	err = db.QueryRow(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxSec).Scan(&g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Update updates the Gmprompt in the database.
func (g *Gmprompt) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if g._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.gmprompt SET (` +
		`gmpfrequency, gmplastactiond, gmplastaction, gmpfreqsetbycust, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4, $5, $6` +
		`) WHERE equinox_lrn = $7`

	// run query
	XOLog(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxSec, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxSec, g.EquinoxLrn)
	return err
}

// Save saves the Gmprompt to the database.
func (g *Gmprompt) Save(db XODB) error {
	if g.Exists() {
		return g.Update(db)
	}

	return g.Insert(db)
}

// Upsert performs an upsert for Gmprompt.
//
// NOTE: PostgreSQL 9.5+ only
func (g *Gmprompt) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.gmprompt (` +
		`gmpfrequency, gmplastactiond, gmplastaction, gmpfreqsetbycust, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6, $7` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`gmpfrequency, gmplastactiond, gmplastaction, gmpfreqsetbycust, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.gmpfrequency, EXCLUDED.gmplastactiond, EXCLUDED.gmplastaction, EXCLUDED.gmpfreqsetbycust, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxLrn, g.EquinoxSec)
	_, err = db.Exec(sqlstr, g.Gmpfrequency, g.Gmplastactiond, g.Gmplastaction, g.Gmpfreqsetbycust, g.EquinoxPrn, g.EquinoxLrn, g.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Delete deletes the Gmprompt from the database.
func (g *Gmprompt) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return nil
	}

	// if deleted, bail
	if g._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.gmprompt WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	g._deleted = true

	return nil
}

// GmpromptByEquinoxLrn retrieves a row from 'equinox.gmprompt' as a Gmprompt.
//
// Generated from index 'gmprompt_pkey'.
func GmpromptByEquinoxLrn(db XODB, equinoxLrn int64) (*Gmprompt, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`gmpfrequency, gmplastactiond, gmplastaction, gmpfreqsetbycust, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.gmprompt ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	g := Gmprompt{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&g.Gmpfrequency, &g.Gmplastactiond, &g.Gmplastaction, &g.Gmpfreqsetbycust, &g.EquinoxPrn, &g.EquinoxLrn, &g.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &g, nil
}
