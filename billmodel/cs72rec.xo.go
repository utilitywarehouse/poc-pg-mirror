// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Cs72rec represents a row from 'equinox.cs72recs'.
type Cs72rec struct {
	Cs72rejreason sql.NullString `json:"cs72rejreason"` // cs72rejreason
	EquinoxPrn    sql.NullInt64  `json:"equinox_prn"`   // equinox_prn
	EquinoxLrn    int64          `json:"equinox_lrn"`   // equinox_lrn
	EquinoxSec    sql.NullInt64  `json:"equinox_sec"`   // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Cs72rec exists in the database.
func (c *Cs72rec) Exists() bool {
	return c._exists
}

// Deleted provides information if the Cs72rec has been deleted from the database.
func (c *Cs72rec) Deleted() bool {
	return c._deleted
}

// Insert inserts the Cs72rec to the database.
func (c *Cs72rec) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cs72recs (` +
		`cs72rejreason, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxSec)
	err = db.QueryRow(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxSec).Scan(&c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Update updates the Cs72rec in the database.
func (c *Cs72rec) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if c._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.cs72recs SET (` +
		`cs72rejreason, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	return err
}

// Save saves the Cs72rec to the database.
func (c *Cs72rec) Save(db XODB) error {
	if c.Exists() {
		return c.Update(db)
	}

	return c.Insert(db)
}

// Upsert performs an upsert for Cs72rec.
//
// NOTE: PostgreSQL 9.5+ only
func (c *Cs72rec) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cs72recs (` +
		`cs72rejreason, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`cs72rejreason, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.cs72rejreason, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	_, err = db.Exec(sqlstr, c.Cs72rejreason, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Delete deletes the Cs72rec from the database.
func (c *Cs72rec) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return nil
	}

	// if deleted, bail
	if c._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.cs72recs WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	c._deleted = true

	return nil
}

// Cs72recByEquinoxLrn retrieves a row from 'equinox.cs72recs' as a Cs72rec.
//
// Generated from index 'cs72recs_pkey'.
func Cs72recByEquinoxLrn(db XODB, equinoxLrn int64) (*Cs72rec, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`cs72rejreason, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.cs72recs ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	c := Cs72rec{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&c.Cs72rejreason, &c.EquinoxPrn, &c.EquinoxLrn, &c.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &c, nil
}
