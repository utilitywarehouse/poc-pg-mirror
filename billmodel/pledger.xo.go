// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Pledger represents a row from 'equinox.pledger'.
type Pledger struct {
	Plpartnerid   sql.NullString  `json:"plpartnerid"`   // plpartnerid
	Ploutstanding sql.NullFloat64 `json:"ploutstanding"` // ploutstanding
	EquinoxLrn    int64           `json:"equinox_lrn"`   // equinox_lrn
	EquinoxSec    sql.NullInt64   `json:"equinox_sec"`   // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Pledger exists in the database.
func (p *Pledger) Exists() bool {
	return p._exists
}

// Deleted provides information if the Pledger has been deleted from the database.
func (p *Pledger) Deleted() bool {
	return p._deleted
}

// Insert inserts the Pledger to the database.
func (p *Pledger) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.pledger (` +
		`plpartnerid, ploutstanding, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxSec)
	err = db.QueryRow(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxSec).Scan(&p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Update updates the Pledger in the database.
func (p *Pledger) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if p._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.pledger SET (` +
		`plpartnerid, ploutstanding, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxSec, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxSec, p.EquinoxLrn)
	return err
}

// Save saves the Pledger to the database.
func (p *Pledger) Save(db XODB) error {
	if p.Exists() {
		return p.Update(db)
	}

	return p.Insert(db)
}

// Upsert performs an upsert for Pledger.
//
// NOTE: PostgreSQL 9.5+ only
func (p *Pledger) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.pledger (` +
		`plpartnerid, ploutstanding, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`plpartnerid, ploutstanding, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.plpartnerid, EXCLUDED.ploutstanding, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxLrn, p.EquinoxSec)
	_, err = db.Exec(sqlstr, p.Plpartnerid, p.Ploutstanding, p.EquinoxLrn, p.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Delete deletes the Pledger from the database.
func (p *Pledger) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return nil
	}

	// if deleted, bail
	if p._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.pledger WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	p._deleted = true

	return nil
}

// PledgerByEquinoxLrn retrieves a row from 'equinox.pledger' as a Pledger.
//
// Generated from index 'pledger_pkey'.
func PledgerByEquinoxLrn(db XODB, equinoxLrn int64) (*Pledger, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`plpartnerid, ploutstanding, equinox_lrn, equinox_sec ` +
		`FROM equinox.pledger ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	p := Pledger{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&p.Plpartnerid, &p.Ploutstanding, &p.EquinoxLrn, &p.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &p, nil
}
