// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// NrgsuppBlb represents a row from 'equinox.nrgsupp_blb'.
type NrgsuppBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the NrgsuppBlb exists in the database.
func (nb *NrgsuppBlb) Exists() bool {
	return nb._exists
}

// Deleted provides information if the NrgsuppBlb has been deleted from the database.
func (nb *NrgsuppBlb) Deleted() bool {
	return nb._deleted
}

// Insert inserts the NrgsuppBlb to the database.
func (nb *NrgsuppBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if nb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.nrgsupp_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, nb.BlbData, nb.BlbText)
	err = db.QueryRow(sqlstr, nb.BlbData, nb.BlbText).Scan(&nb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	nb._exists = true

	return nil
}

// Update updates the NrgsuppBlb in the database.
func (nb *NrgsuppBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !nb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if nb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.nrgsupp_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, nb.BlbData, nb.BlbText, nb.BlbLrn)
	_, err = db.Exec(sqlstr, nb.BlbData, nb.BlbText, nb.BlbLrn)
	return err
}

// Save saves the NrgsuppBlb to the database.
func (nb *NrgsuppBlb) Save(db XODB) error {
	if nb.Exists() {
		return nb.Update(db)
	}

	return nb.Insert(db)
}

// Upsert performs an upsert for NrgsuppBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (nb *NrgsuppBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if nb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.nrgsupp_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, nb.BlbLrn, nb.BlbData, nb.BlbText)
	_, err = db.Exec(sqlstr, nb.BlbLrn, nb.BlbData, nb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	nb._exists = true

	return nil
}

// Delete deletes the NrgsuppBlb from the database.
func (nb *NrgsuppBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !nb._exists {
		return nil
	}

	// if deleted, bail
	if nb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.nrgsupp_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, nb.BlbLrn)
	_, err = db.Exec(sqlstr, nb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	nb._deleted = true

	return nil
}

// NrgsuppBlbByBlbLrn retrieves a row from 'equinox.nrgsupp_blb' as a NrgsuppBlb.
//
// Generated from index 'nrgsupp_blb_pkey'.
func NrgsuppBlbByBlbLrn(db XODB, blbLrn int64) (*NrgsuppBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.nrgsupp_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	nb := NrgsuppBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&nb.BlbLrn, &nb.BlbData, &nb.BlbText)
	if err != nil {
		return nil, err
	}

	return &nb, nil
}
