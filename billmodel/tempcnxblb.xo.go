// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// TempCnxBlb represents a row from 'equinox.temp_cnx_blb'.
type TempCnxBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the TempCnxBlb exists in the database.
func (tcb *TempCnxBlb) Exists() bool {
	return tcb._exists
}

// Deleted provides information if the TempCnxBlb has been deleted from the database.
func (tcb *TempCnxBlb) Deleted() bool {
	return tcb._deleted
}

// Insert inserts the TempCnxBlb to the database.
func (tcb *TempCnxBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if tcb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.temp_cnx_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, tcb.BlbData, tcb.BlbText)
	err = db.QueryRow(sqlstr, tcb.BlbData, tcb.BlbText).Scan(&tcb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	tcb._exists = true

	return nil
}

// Update updates the TempCnxBlb in the database.
func (tcb *TempCnxBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !tcb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if tcb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.temp_cnx_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, tcb.BlbData, tcb.BlbText, tcb.BlbLrn)
	_, err = db.Exec(sqlstr, tcb.BlbData, tcb.BlbText, tcb.BlbLrn)
	return err
}

// Save saves the TempCnxBlb to the database.
func (tcb *TempCnxBlb) Save(db XODB) error {
	if tcb.Exists() {
		return tcb.Update(db)
	}

	return tcb.Insert(db)
}

// Upsert performs an upsert for TempCnxBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (tcb *TempCnxBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if tcb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.temp_cnx_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, tcb.BlbLrn, tcb.BlbData, tcb.BlbText)
	_, err = db.Exec(sqlstr, tcb.BlbLrn, tcb.BlbData, tcb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	tcb._exists = true

	return nil
}

// Delete deletes the TempCnxBlb from the database.
func (tcb *TempCnxBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !tcb._exists {
		return nil
	}

	// if deleted, bail
	if tcb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.temp_cnx_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, tcb.BlbLrn)
	_, err = db.Exec(sqlstr, tcb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	tcb._deleted = true

	return nil
}

// TempCnxBlbByBlbLrn retrieves a row from 'equinox.temp_cnx_blb' as a TempCnxBlb.
//
// Generated from index 'temp_cnx_blb_pkey'.
func TempCnxBlbByBlbLrn(db XODB, blbLrn int64) (*TempCnxBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.temp_cnx_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	tcb := TempCnxBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&tcb.BlbLrn, &tcb.BlbData, &tcb.BlbText)
	if err != nil {
		return nil, err
	}

	return &tcb, nil
}
