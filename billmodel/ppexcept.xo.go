// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Ppexcept represents a row from 'equinox.ppexcept'.
type Ppexcept struct {
	Ppexcode     sql.NullString `json:"ppexcode"`     // ppexcode
	Ppexdescript sql.NullString `json:"ppexdescript"` // ppexdescript
	EquinoxLrn   int64          `json:"equinox_lrn"`  // equinox_lrn
	EquinoxSec   sql.NullInt64  `json:"equinox_sec"`  // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Ppexcept exists in the database.
func (p *Ppexcept) Exists() bool {
	return p._exists
}

// Deleted provides information if the Ppexcept has been deleted from the database.
func (p *Ppexcept) Deleted() bool {
	return p._deleted
}

// Insert inserts the Ppexcept to the database.
func (p *Ppexcept) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ppexcept (` +
		`ppexcode, ppexdescript, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxSec)
	err = db.QueryRow(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxSec).Scan(&p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Update updates the Ppexcept in the database.
func (p *Ppexcept) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if p._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ppexcept SET (` +
		`ppexcode, ppexdescript, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxSec, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxSec, p.EquinoxLrn)
	return err
}

// Save saves the Ppexcept to the database.
func (p *Ppexcept) Save(db XODB) error {
	if p.Exists() {
		return p.Update(db)
	}

	return p.Insert(db)
}

// Upsert performs an upsert for Ppexcept.
//
// NOTE: PostgreSQL 9.5+ only
func (p *Ppexcept) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ppexcept (` +
		`ppexcode, ppexdescript, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`ppexcode, ppexdescript, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.ppexcode, EXCLUDED.ppexdescript, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxLrn, p.EquinoxSec)
	_, err = db.Exec(sqlstr, p.Ppexcode, p.Ppexdescript, p.EquinoxLrn, p.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Delete deletes the Ppexcept from the database.
func (p *Ppexcept) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return nil
	}

	// if deleted, bail
	if p._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ppexcept WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	p._deleted = true

	return nil
}

// PpexceptByEquinoxLrn retrieves a row from 'equinox.ppexcept' as a Ppexcept.
//
// Generated from index 'ppexcept_pkey'.
func PpexceptByEquinoxLrn(db XODB, equinoxLrn int64) (*Ppexcept, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`ppexcode, ppexdescript, equinox_lrn, equinox_sec ` +
		`FROM equinox.ppexcept ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	p := Ppexcept{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&p.Ppexcode, &p.Ppexdescript, &p.EquinoxLrn, &p.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &p, nil
}
