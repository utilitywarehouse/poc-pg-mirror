// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Cntcount represents a row from 'equinox.cntcount'.
type Cntcount struct {
	Cntcmonth  sql.NullString `json:"cntcmonth"`   // cntcmonth
	Cntccount  sql.NullInt64  `json:"cntccount"`   // cntccount
	EquinoxPrn sql.NullInt64  `json:"equinox_prn"` // equinox_prn
	EquinoxLrn int64          `json:"equinox_lrn"` // equinox_lrn
	EquinoxSec sql.NullInt64  `json:"equinox_sec"` // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Cntcount exists in the database.
func (c *Cntcount) Exists() bool {
	return c._exists
}

// Deleted provides information if the Cntcount has been deleted from the database.
func (c *Cntcount) Deleted() bool {
	return c._deleted
}

// Insert inserts the Cntcount to the database.
func (c *Cntcount) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cntcount (` +
		`cntcmonth, cntccount, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxSec)
	err = db.QueryRow(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxSec).Scan(&c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Update updates the Cntcount in the database.
func (c *Cntcount) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if c._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.cntcount SET (` +
		`cntcmonth, cntccount, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4` +
		`) WHERE equinox_lrn = $5`

	// run query
	XOLog(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	return err
}

// Save saves the Cntcount to the database.
func (c *Cntcount) Save(db XODB) error {
	if c.Exists() {
		return c.Update(db)
	}

	return c.Insert(db)
}

// Upsert performs an upsert for Cntcount.
//
// NOTE: PostgreSQL 9.5+ only
func (c *Cntcount) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cntcount (` +
		`cntcmonth, cntccount, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`cntcmonth, cntccount, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.cntcmonth, EXCLUDED.cntccount, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	_, err = db.Exec(sqlstr, c.Cntcmonth, c.Cntccount, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Delete deletes the Cntcount from the database.
func (c *Cntcount) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return nil
	}

	// if deleted, bail
	if c._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.cntcount WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	c._deleted = true

	return nil
}

// CntcountByEquinoxLrn retrieves a row from 'equinox.cntcount' as a Cntcount.
//
// Generated from index 'cntcount_pkey'.
func CntcountByEquinoxLrn(db XODB, equinoxLrn int64) (*Cntcount, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`cntcmonth, cntccount, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.cntcount ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	c := Cntcount{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&c.Cntcmonth, &c.Cntccount, &c.EquinoxPrn, &c.EquinoxLrn, &c.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &c, nil
}
