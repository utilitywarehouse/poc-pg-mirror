// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// IgtconBlb represents a row from 'equinox.igtcon_blb'.
type IgtconBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the IgtconBlb exists in the database.
func (ib *IgtconBlb) Exists() bool {
	return ib._exists
}

// Deleted provides information if the IgtconBlb has been deleted from the database.
func (ib *IgtconBlb) Deleted() bool {
	return ib._deleted
}

// Insert inserts the IgtconBlb to the database.
func (ib *IgtconBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if ib._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.igtcon_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, ib.BlbData, ib.BlbText)
	err = db.QueryRow(sqlstr, ib.BlbData, ib.BlbText).Scan(&ib.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	ib._exists = true

	return nil
}

// Update updates the IgtconBlb in the database.
func (ib *IgtconBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ib._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if ib._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.igtcon_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, ib.BlbData, ib.BlbText, ib.BlbLrn)
	_, err = db.Exec(sqlstr, ib.BlbData, ib.BlbText, ib.BlbLrn)
	return err
}

// Save saves the IgtconBlb to the database.
func (ib *IgtconBlb) Save(db XODB) error {
	if ib.Exists() {
		return ib.Update(db)
	}

	return ib.Insert(db)
}

// Upsert performs an upsert for IgtconBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (ib *IgtconBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if ib._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.igtcon_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, ib.BlbLrn, ib.BlbData, ib.BlbText)
	_, err = db.Exec(sqlstr, ib.BlbLrn, ib.BlbData, ib.BlbText)
	if err != nil {
		return err
	}

	// set existence
	ib._exists = true

	return nil
}

// Delete deletes the IgtconBlb from the database.
func (ib *IgtconBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ib._exists {
		return nil
	}

	// if deleted, bail
	if ib._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.igtcon_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, ib.BlbLrn)
	_, err = db.Exec(sqlstr, ib.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	ib._deleted = true

	return nil
}

// IgtconBlbByBlbLrn retrieves a row from 'equinox.igtcon_blb' as a IgtconBlb.
//
// Generated from index 'igtcon_blb_pkey'.
func IgtconBlbByBlbLrn(db XODB, blbLrn int64) (*IgtconBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.igtcon_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	ib := IgtconBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&ib.BlbLrn, &ib.BlbData, &ib.BlbText)
	if err != nil {
		return nil, err
	}

	return &ib, nil
}
