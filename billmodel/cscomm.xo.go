// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"

	"github.com/lib/pq"
)

// Cscomm represents a row from 'equinox.cscomm'.
type Cscomm struct {
	Cscomment     sql.NullInt64  `json:"cscomment"`     // cscomment
	Cscommdate    pq.NullTime    `json:"cscommdate"`    // cscommdate
	Cscommentered sql.NullString `json:"cscommentered"` // cscommentered
	EquinoxPrn    sql.NullInt64  `json:"equinox_prn"`   // equinox_prn
	EquinoxLrn    int64          `json:"equinox_lrn"`   // equinox_lrn
	EquinoxSec    sql.NullInt64  `json:"equinox_sec"`   // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Cscomm exists in the database.
func (c *Cscomm) Exists() bool {
	return c._exists
}

// Deleted provides information if the Cscomm has been deleted from the database.
func (c *Cscomm) Deleted() bool {
	return c._deleted
}

// Insert inserts the Cscomm to the database.
func (c *Cscomm) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cscomm (` +
		`cscomment, cscommdate, cscommentered, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxSec)
	err = db.QueryRow(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxSec).Scan(&c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Update updates the Cscomm in the database.
func (c *Cscomm) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if c._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.cscomm SET (` +
		`cscomment, cscommdate, cscommentered, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4, $5` +
		`) WHERE equinox_lrn = $6`

	// run query
	XOLog(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	return err
}

// Save saves the Cscomm to the database.
func (c *Cscomm) Save(db XODB) error {
	if c.Exists() {
		return c.Update(db)
	}

	return c.Insert(db)
}

// Upsert performs an upsert for Cscomm.
//
// NOTE: PostgreSQL 9.5+ only
func (c *Cscomm) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cscomm (` +
		`cscomment, cscommdate, cscommentered, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`cscomment, cscommdate, cscommentered, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.cscomment, EXCLUDED.cscommdate, EXCLUDED.cscommentered, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	_, err = db.Exec(sqlstr, c.Cscomment, c.Cscommdate, c.Cscommentered, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Delete deletes the Cscomm from the database.
func (c *Cscomm) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return nil
	}

	// if deleted, bail
	if c._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.cscomm WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	c._deleted = true

	return nil
}

// CscommByEquinoxLrn retrieves a row from 'equinox.cscomm' as a Cscomm.
//
// Generated from index 'cscomm_pkey'.
func CscommByEquinoxLrn(db XODB, equinoxLrn int64) (*Cscomm, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`cscomment, cscommdate, cscommentered, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.cscomm ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	c := Cscomm{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&c.Cscomment, &c.Cscommdate, &c.Cscommentered, &c.EquinoxPrn, &c.EquinoxLrn, &c.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &c, nil
}
