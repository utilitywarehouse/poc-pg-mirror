// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// AclobjBlb represents a row from 'equinox.aclobj_blb'.
type AclobjBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the AclobjBlb exists in the database.
func (ab *AclobjBlb) Exists() bool {
	return ab._exists
}

// Deleted provides information if the AclobjBlb has been deleted from the database.
func (ab *AclobjBlb) Deleted() bool {
	return ab._deleted
}

// Insert inserts the AclobjBlb to the database.
func (ab *AclobjBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if ab._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.aclobj_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, ab.BlbData, ab.BlbText)
	err = db.QueryRow(sqlstr, ab.BlbData, ab.BlbText).Scan(&ab.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	ab._exists = true

	return nil
}

// Update updates the AclobjBlb in the database.
func (ab *AclobjBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ab._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if ab._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.aclobj_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, ab.BlbData, ab.BlbText, ab.BlbLrn)
	_, err = db.Exec(sqlstr, ab.BlbData, ab.BlbText, ab.BlbLrn)
	return err
}

// Save saves the AclobjBlb to the database.
func (ab *AclobjBlb) Save(db XODB) error {
	if ab.Exists() {
		return ab.Update(db)
	}

	return ab.Insert(db)
}

// Upsert performs an upsert for AclobjBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (ab *AclobjBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if ab._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.aclobj_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, ab.BlbLrn, ab.BlbData, ab.BlbText)
	_, err = db.Exec(sqlstr, ab.BlbLrn, ab.BlbData, ab.BlbText)
	if err != nil {
		return err
	}

	// set existence
	ab._exists = true

	return nil
}

// Delete deletes the AclobjBlb from the database.
func (ab *AclobjBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ab._exists {
		return nil
	}

	// if deleted, bail
	if ab._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.aclobj_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, ab.BlbLrn)
	_, err = db.Exec(sqlstr, ab.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	ab._deleted = true

	return nil
}

// AclobjBlbByBlbLrn retrieves a row from 'equinox.aclobj_blb' as a AclobjBlb.
//
// Generated from index 'aclobj_blb_pkey'.
func AclobjBlbByBlbLrn(db XODB, blbLrn int64) (*AclobjBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.aclobj_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	ab := AclobjBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&ab.BlbLrn, &ab.BlbData, &ab.BlbText)
	if err != nil {
		return nil, err
	}

	return &ab, nil
}
