// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// ValccodeBlb represents a row from 'equinox.valccode_blb'.
type ValccodeBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the ValccodeBlb exists in the database.
func (vb *ValccodeBlb) Exists() bool {
	return vb._exists
}

// Deleted provides information if the ValccodeBlb has been deleted from the database.
func (vb *ValccodeBlb) Deleted() bool {
	return vb._deleted
}

// Insert inserts the ValccodeBlb to the database.
func (vb *ValccodeBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if vb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.valccode_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, vb.BlbData, vb.BlbText)
	err = db.QueryRow(sqlstr, vb.BlbData, vb.BlbText).Scan(&vb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	vb._exists = true

	return nil
}

// Update updates the ValccodeBlb in the database.
func (vb *ValccodeBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !vb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if vb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.valccode_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, vb.BlbData, vb.BlbText, vb.BlbLrn)
	_, err = db.Exec(sqlstr, vb.BlbData, vb.BlbText, vb.BlbLrn)
	return err
}

// Save saves the ValccodeBlb to the database.
func (vb *ValccodeBlb) Save(db XODB) error {
	if vb.Exists() {
		return vb.Update(db)
	}

	return vb.Insert(db)
}

// Upsert performs an upsert for ValccodeBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (vb *ValccodeBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if vb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.valccode_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, vb.BlbLrn, vb.BlbData, vb.BlbText)
	_, err = db.Exec(sqlstr, vb.BlbLrn, vb.BlbData, vb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	vb._exists = true

	return nil
}

// Delete deletes the ValccodeBlb from the database.
func (vb *ValccodeBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !vb._exists {
		return nil
	}

	// if deleted, bail
	if vb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.valccode_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, vb.BlbLrn)
	_, err = db.Exec(sqlstr, vb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	vb._deleted = true

	return nil
}

// ValccodeBlbByBlbLrn retrieves a row from 'equinox.valccode_blb' as a ValccodeBlb.
//
// Generated from index 'valccode_blb_pkey'.
func ValccodeBlbByBlbLrn(db XODB, blbLrn int64) (*ValccodeBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.valccode_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	vb := ValccodeBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&vb.BlbLrn, &vb.BlbData, &vb.BlbText)
	if err != nil {
		return nil, err
	}

	return &vb, nil
}
