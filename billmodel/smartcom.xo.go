// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"

	"github.com/lib/pq"
)

// Smartcom represents a row from 'equinox.smartcom'.
type Smartcom struct {
	Smcid      sql.NullString `json:"smcid"`       // smcid
	Smcdate    pq.NullTime    `json:"smcdate"`     // smcdate
	Smcsentby  sql.NullString `json:"smcsentby"`   // smcsentby
	Smctype    sql.NullInt64  `json:"smctype"`     // smctype
	EquinoxPrn sql.NullInt64  `json:"equinox_prn"` // equinox_prn
	EquinoxLrn int64          `json:"equinox_lrn"` // equinox_lrn
	EquinoxSec sql.NullInt64  `json:"equinox_sec"` // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Smartcom exists in the database.
func (s *Smartcom) Exists() bool {
	return s._exists
}

// Deleted provides information if the Smartcom has been deleted from the database.
func (s *Smartcom) Deleted() bool {
	return s._deleted
}

// Insert inserts the Smartcom to the database.
func (s *Smartcom) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if s._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.smartcom (` +
		`smcid, smcdate, smcsentby, smctype, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxSec)
	err = db.QueryRow(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxSec).Scan(&s.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	s._exists = true

	return nil
}

// Update updates the Smartcom in the database.
func (s *Smartcom) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !s._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if s._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.smartcom SET (` +
		`smcid, smcdate, smcsentby, smctype, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4, $5, $6` +
		`) WHERE equinox_lrn = $7`

	// run query
	XOLog(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxSec, s.EquinoxLrn)
	_, err = db.Exec(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxSec, s.EquinoxLrn)
	return err
}

// Save saves the Smartcom to the database.
func (s *Smartcom) Save(db XODB) error {
	if s.Exists() {
		return s.Update(db)
	}

	return s.Insert(db)
}

// Upsert performs an upsert for Smartcom.
//
// NOTE: PostgreSQL 9.5+ only
func (s *Smartcom) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if s._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.smartcom (` +
		`smcid, smcdate, smcsentby, smctype, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6, $7` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`smcid, smcdate, smcsentby, smctype, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.smcid, EXCLUDED.smcdate, EXCLUDED.smcsentby, EXCLUDED.smctype, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxLrn, s.EquinoxSec)
	_, err = db.Exec(sqlstr, s.Smcid, s.Smcdate, s.Smcsentby, s.Smctype, s.EquinoxPrn, s.EquinoxLrn, s.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	s._exists = true

	return nil
}

// Delete deletes the Smartcom from the database.
func (s *Smartcom) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !s._exists {
		return nil
	}

	// if deleted, bail
	if s._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.smartcom WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, s.EquinoxLrn)
	_, err = db.Exec(sqlstr, s.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	s._deleted = true

	return nil
}

// SmartcomByEquinoxLrn retrieves a row from 'equinox.smartcom' as a Smartcom.
//
// Generated from index 'smartcom_pkey'.
func SmartcomByEquinoxLrn(db XODB, equinoxLrn int64) (*Smartcom, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`smcid, smcdate, smcsentby, smctype, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.smartcom ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	s := Smartcom{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&s.Smcid, &s.Smcdate, &s.Smcsentby, &s.Smctype, &s.EquinoxPrn, &s.EquinoxLrn, &s.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &s, nil
}
