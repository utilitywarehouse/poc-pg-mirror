// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Ps71rec represents a row from 'equinox.ps71recs'.
type Ps71rec struct {
	Ps71rejfilename sql.NullString `json:"ps71rejfilename"` // ps71rejfilename
	EquinoxPrn      sql.NullInt64  `json:"equinox_prn"`     // equinox_prn
	EquinoxLrn      int64          `json:"equinox_lrn"`     // equinox_lrn
	EquinoxSec      sql.NullInt64  `json:"equinox_sec"`     // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Ps71rec exists in the database.
func (p *Ps71rec) Exists() bool {
	return p._exists
}

// Deleted provides information if the Ps71rec has been deleted from the database.
func (p *Ps71rec) Deleted() bool {
	return p._deleted
}

// Insert inserts the Ps71rec to the database.
func (p *Ps71rec) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ps71recs (` +
		`ps71rejfilename, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxSec)
	err = db.QueryRow(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxSec).Scan(&p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Update updates the Ps71rec in the database.
func (p *Ps71rec) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if p._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ps71recs SET (` +
		`ps71rejfilename, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxSec, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxSec, p.EquinoxLrn)
	return err
}

// Save saves the Ps71rec to the database.
func (p *Ps71rec) Save(db XODB) error {
	if p.Exists() {
		return p.Update(db)
	}

	return p.Insert(db)
}

// Upsert performs an upsert for Ps71rec.
//
// NOTE: PostgreSQL 9.5+ only
func (p *Ps71rec) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if p._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ps71recs (` +
		`ps71rejfilename, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`ps71rejfilename, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.ps71rejfilename, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxLrn, p.EquinoxSec)
	_, err = db.Exec(sqlstr, p.Ps71rejfilename, p.EquinoxPrn, p.EquinoxLrn, p.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	p._exists = true

	return nil
}

// Delete deletes the Ps71rec from the database.
func (p *Ps71rec) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !p._exists {
		return nil
	}

	// if deleted, bail
	if p._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ps71recs WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, p.EquinoxLrn)
	_, err = db.Exec(sqlstr, p.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	p._deleted = true

	return nil
}

// Ps71recByEquinoxLrn retrieves a row from 'equinox.ps71recs' as a Ps71rec.
//
// Generated from index 'ps71recs_pkey'.
func Ps71recByEquinoxLrn(db XODB, equinoxLrn int64) (*Ps71rec, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`ps71rejfilename, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.ps71recs ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	p := Ps71rec{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&p.Ps71rejfilename, &p.EquinoxPrn, &p.EquinoxLrn, &p.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &p, nil
}
