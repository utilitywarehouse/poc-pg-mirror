// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// UdcmainBlb represents a row from 'equinox.udcmain_blb'.
type UdcmainBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the UdcmainBlb exists in the database.
func (ub *UdcmainBlb) Exists() bool {
	return ub._exists
}

// Deleted provides information if the UdcmainBlb has been deleted from the database.
func (ub *UdcmainBlb) Deleted() bool {
	return ub._deleted
}

// Insert inserts the UdcmainBlb to the database.
func (ub *UdcmainBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if ub._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.udcmain_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, ub.BlbData, ub.BlbText)
	err = db.QueryRow(sqlstr, ub.BlbData, ub.BlbText).Scan(&ub.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	ub._exists = true

	return nil
}

// Update updates the UdcmainBlb in the database.
func (ub *UdcmainBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ub._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if ub._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.udcmain_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, ub.BlbData, ub.BlbText, ub.BlbLrn)
	_, err = db.Exec(sqlstr, ub.BlbData, ub.BlbText, ub.BlbLrn)
	return err
}

// Save saves the UdcmainBlb to the database.
func (ub *UdcmainBlb) Save(db XODB) error {
	if ub.Exists() {
		return ub.Update(db)
	}

	return ub.Insert(db)
}

// Upsert performs an upsert for UdcmainBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (ub *UdcmainBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if ub._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.udcmain_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, ub.BlbLrn, ub.BlbData, ub.BlbText)
	_, err = db.Exec(sqlstr, ub.BlbLrn, ub.BlbData, ub.BlbText)
	if err != nil {
		return err
	}

	// set existence
	ub._exists = true

	return nil
}

// Delete deletes the UdcmainBlb from the database.
func (ub *UdcmainBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ub._exists {
		return nil
	}

	// if deleted, bail
	if ub._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.udcmain_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, ub.BlbLrn)
	_, err = db.Exec(sqlstr, ub.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	ub._deleted = true

	return nil
}

// UdcmainBlbByBlbLrn retrieves a row from 'equinox.udcmain_blb' as a UdcmainBlb.
//
// Generated from index 'udcmain_blb_pkey'.
func UdcmainBlbByBlbLrn(db XODB, blbLrn int64) (*UdcmainBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.udcmain_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	ub := UdcmainBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&ub.BlbLrn, &ub.BlbData, &ub.BlbText)
	if err != nil {
		return nil, err
	}

	return &ub, nil
}
