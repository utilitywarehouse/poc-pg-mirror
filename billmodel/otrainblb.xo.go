// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// OTrainBlb represents a row from 'equinox.o_train_blb'.
type OTrainBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the OTrainBlb exists in the database.
func (otb *OTrainBlb) Exists() bool {
	return otb._exists
}

// Deleted provides information if the OTrainBlb has been deleted from the database.
func (otb *OTrainBlb) Deleted() bool {
	return otb._deleted
}

// Insert inserts the OTrainBlb to the database.
func (otb *OTrainBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if otb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.o_train_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, otb.BlbData, otb.BlbText)
	err = db.QueryRow(sqlstr, otb.BlbData, otb.BlbText).Scan(&otb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	otb._exists = true

	return nil
}

// Update updates the OTrainBlb in the database.
func (otb *OTrainBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !otb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if otb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.o_train_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, otb.BlbData, otb.BlbText, otb.BlbLrn)
	_, err = db.Exec(sqlstr, otb.BlbData, otb.BlbText, otb.BlbLrn)
	return err
}

// Save saves the OTrainBlb to the database.
func (otb *OTrainBlb) Save(db XODB) error {
	if otb.Exists() {
		return otb.Update(db)
	}

	return otb.Insert(db)
}

// Upsert performs an upsert for OTrainBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (otb *OTrainBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if otb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.o_train_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, otb.BlbLrn, otb.BlbData, otb.BlbText)
	_, err = db.Exec(sqlstr, otb.BlbLrn, otb.BlbData, otb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	otb._exists = true

	return nil
}

// Delete deletes the OTrainBlb from the database.
func (otb *OTrainBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !otb._exists {
		return nil
	}

	// if deleted, bail
	if otb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.o_train_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, otb.BlbLrn)
	_, err = db.Exec(sqlstr, otb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	otb._deleted = true

	return nil
}

// OTrainBlbByBlbLrn retrieves a row from 'equinox.o_train_blb' as a OTrainBlb.
//
// Generated from index 'o_train_blb_pkey'.
func OTrainBlbByBlbLrn(db XODB, blbLrn int64) (*OTrainBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.o_train_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	otb := OTrainBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&otb.BlbLrn, &otb.BlbData, &otb.BlbText)
	if err != nil {
		return nil, err
	}

	return &otb, nil
}
