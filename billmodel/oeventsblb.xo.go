// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// OEventsBlb represents a row from 'equinox.o_events_blb'.
type OEventsBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the OEventsBlb exists in the database.
func (oeb *OEventsBlb) Exists() bool {
	return oeb._exists
}

// Deleted provides information if the OEventsBlb has been deleted from the database.
func (oeb *OEventsBlb) Deleted() bool {
	return oeb._deleted
}

// Insert inserts the OEventsBlb to the database.
func (oeb *OEventsBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if oeb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.o_events_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, oeb.BlbData, oeb.BlbText)
	err = db.QueryRow(sqlstr, oeb.BlbData, oeb.BlbText).Scan(&oeb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	oeb._exists = true

	return nil
}

// Update updates the OEventsBlb in the database.
func (oeb *OEventsBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !oeb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if oeb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.o_events_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, oeb.BlbData, oeb.BlbText, oeb.BlbLrn)
	_, err = db.Exec(sqlstr, oeb.BlbData, oeb.BlbText, oeb.BlbLrn)
	return err
}

// Save saves the OEventsBlb to the database.
func (oeb *OEventsBlb) Save(db XODB) error {
	if oeb.Exists() {
		return oeb.Update(db)
	}

	return oeb.Insert(db)
}

// Upsert performs an upsert for OEventsBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (oeb *OEventsBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if oeb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.o_events_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, oeb.BlbLrn, oeb.BlbData, oeb.BlbText)
	_, err = db.Exec(sqlstr, oeb.BlbLrn, oeb.BlbData, oeb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	oeb._exists = true

	return nil
}

// Delete deletes the OEventsBlb from the database.
func (oeb *OEventsBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !oeb._exists {
		return nil
	}

	// if deleted, bail
	if oeb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.o_events_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, oeb.BlbLrn)
	_, err = db.Exec(sqlstr, oeb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	oeb._deleted = true

	return nil
}

// OEventsBlbByBlbLrn retrieves a row from 'equinox.o_events_blb' as a OEventsBlb.
//
// Generated from index 'o_events_blb_pkey'.
func OEventsBlbByBlbLrn(db XODB, blbLrn int64) (*OEventsBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.o_events_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	oeb := OEventsBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&oeb.BlbLrn, &oeb.BlbData, &oeb.BlbText)
	if err != nil {
		return nil, err
	}

	return &oeb, nil
}
