// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// FfdfileBlb represents a row from 'equinox.ffdfile_blb'.
type FfdfileBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the FfdfileBlb exists in the database.
func (fb *FfdfileBlb) Exists() bool {
	return fb._exists
}

// Deleted provides information if the FfdfileBlb has been deleted from the database.
func (fb *FfdfileBlb) Deleted() bool {
	return fb._deleted
}

// Insert inserts the FfdfileBlb to the database.
func (fb *FfdfileBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if fb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ffdfile_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, fb.BlbData, fb.BlbText)
	err = db.QueryRow(sqlstr, fb.BlbData, fb.BlbText).Scan(&fb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	fb._exists = true

	return nil
}

// Update updates the FfdfileBlb in the database.
func (fb *FfdfileBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !fb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if fb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ffdfile_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, fb.BlbData, fb.BlbText, fb.BlbLrn)
	_, err = db.Exec(sqlstr, fb.BlbData, fb.BlbText, fb.BlbLrn)
	return err
}

// Save saves the FfdfileBlb to the database.
func (fb *FfdfileBlb) Save(db XODB) error {
	if fb.Exists() {
		return fb.Update(db)
	}

	return fb.Insert(db)
}

// Upsert performs an upsert for FfdfileBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (fb *FfdfileBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if fb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ffdfile_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, fb.BlbLrn, fb.BlbData, fb.BlbText)
	_, err = db.Exec(sqlstr, fb.BlbLrn, fb.BlbData, fb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	fb._exists = true

	return nil
}

// Delete deletes the FfdfileBlb from the database.
func (fb *FfdfileBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !fb._exists {
		return nil
	}

	// if deleted, bail
	if fb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ffdfile_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, fb.BlbLrn)
	_, err = db.Exec(sqlstr, fb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	fb._deleted = true

	return nil
}

// FfdfileBlbByBlbLrn retrieves a row from 'equinox.ffdfile_blb' as a FfdfileBlb.
//
// Generated from index 'ffdfile_blb_pkey'.
func FfdfileBlbByBlbLrn(db XODB, blbLrn int64) (*FfdfileBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.ffdfile_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	fb := FfdfileBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&fb.BlbLrn, &fb.BlbData, &fb.BlbText)
	if err != nil {
		return nil, err
	}

	return &fb, nil
}
