// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"

	"github.com/lib/pq"
)

// Gmorrej represents a row from 'equinox.gmorrej'.
type Gmorrej struct {
	Gmorrejcode   sql.NullString `json:"gmorrejcode"`   // gmorrejcode
	Gmorrejreason sql.NullString `json:"gmorrejreason"` // gmorrejreason
	Gmorrejdate   pq.NullTime    `json:"gmorrejdate"`   // gmorrejdate
	EquinoxPrn    sql.NullInt64  `json:"equinox_prn"`   // equinox_prn
	EquinoxLrn    int64          `json:"equinox_lrn"`   // equinox_lrn
	EquinoxSec    sql.NullInt64  `json:"equinox_sec"`   // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Gmorrej exists in the database.
func (g *Gmorrej) Exists() bool {
	return g._exists
}

// Deleted provides information if the Gmorrej has been deleted from the database.
func (g *Gmorrej) Deleted() bool {
	return g._deleted
}

// Insert inserts the Gmorrej to the database.
func (g *Gmorrej) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.gmorrej (` +
		`gmorrejcode, gmorrejreason, gmorrejdate, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxSec)
	err = db.QueryRow(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxSec).Scan(&g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Update updates the Gmorrej in the database.
func (g *Gmorrej) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if g._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.gmorrej SET (` +
		`gmorrejcode, gmorrejreason, gmorrejdate, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4, $5` +
		`) WHERE equinox_lrn = $6`

	// run query
	XOLog(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxSec, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxSec, g.EquinoxLrn)
	return err
}

// Save saves the Gmorrej to the database.
func (g *Gmorrej) Save(db XODB) error {
	if g.Exists() {
		return g.Update(db)
	}

	return g.Insert(db)
}

// Upsert performs an upsert for Gmorrej.
//
// NOTE: PostgreSQL 9.5+ only
func (g *Gmorrej) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.gmorrej (` +
		`gmorrejcode, gmorrejreason, gmorrejdate, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`gmorrejcode, gmorrejreason, gmorrejdate, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.gmorrejcode, EXCLUDED.gmorrejreason, EXCLUDED.gmorrejdate, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxLrn, g.EquinoxSec)
	_, err = db.Exec(sqlstr, g.Gmorrejcode, g.Gmorrejreason, g.Gmorrejdate, g.EquinoxPrn, g.EquinoxLrn, g.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Delete deletes the Gmorrej from the database.
func (g *Gmorrej) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return nil
	}

	// if deleted, bail
	if g._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.gmorrej WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	g._deleted = true

	return nil
}

// GmorrejByEquinoxLrn retrieves a row from 'equinox.gmorrej' as a Gmorrej.
//
// Generated from index 'gmorrej_pkey'.
func GmorrejByEquinoxLrn(db XODB, equinoxLrn int64) (*Gmorrej, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`gmorrejcode, gmorrejreason, gmorrejdate, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.gmorrej ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	g := Gmorrej{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&g.Gmorrejcode, &g.Gmorrejreason, &g.Gmorrejdate, &g.EquinoxPrn, &g.EquinoxLrn, &g.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &g, nil
}
