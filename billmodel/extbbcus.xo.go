// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Extbbcus represents a row from 'equinox.extbbcus'.
type Extbbcus struct {
	Extbbcustaccount sql.NullString `json:"extbbcustaccount"` // extbbcustaccount
	EquinoxPrn       sql.NullInt64  `json:"equinox_prn"`      // equinox_prn
	EquinoxLrn       int64          `json:"equinox_lrn"`      // equinox_lrn
	EquinoxSec       sql.NullInt64  `json:"equinox_sec"`      // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Extbbcus exists in the database.
func (e *Extbbcus) Exists() bool {
	return e._exists
}

// Deleted provides information if the Extbbcus has been deleted from the database.
func (e *Extbbcus) Deleted() bool {
	return e._deleted
}

// Insert inserts the Extbbcus to the database.
func (e *Extbbcus) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if e._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.extbbcus (` +
		`extbbcustaccount, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxSec)
	err = db.QueryRow(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxSec).Scan(&e.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	e._exists = true

	return nil
}

// Update updates the Extbbcus in the database.
func (e *Extbbcus) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !e._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if e._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.extbbcus SET (` +
		`extbbcustaccount, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxSec, e.EquinoxLrn)
	_, err = db.Exec(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxSec, e.EquinoxLrn)
	return err
}

// Save saves the Extbbcus to the database.
func (e *Extbbcus) Save(db XODB) error {
	if e.Exists() {
		return e.Update(db)
	}

	return e.Insert(db)
}

// Upsert performs an upsert for Extbbcus.
//
// NOTE: PostgreSQL 9.5+ only
func (e *Extbbcus) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if e._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.extbbcus (` +
		`extbbcustaccount, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`extbbcustaccount, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.extbbcustaccount, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxLrn, e.EquinoxSec)
	_, err = db.Exec(sqlstr, e.Extbbcustaccount, e.EquinoxPrn, e.EquinoxLrn, e.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	e._exists = true

	return nil
}

// Delete deletes the Extbbcus from the database.
func (e *Extbbcus) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !e._exists {
		return nil
	}

	// if deleted, bail
	if e._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.extbbcus WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, e.EquinoxLrn)
	_, err = db.Exec(sqlstr, e.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	e._deleted = true

	return nil
}

// ExtbbcusByEquinoxLrn retrieves a row from 'equinox.extbbcus' as a Extbbcus.
//
// Generated from index 'extbbcus_pkey'.
func ExtbbcusByEquinoxLrn(db XODB, equinoxLrn int64) (*Extbbcus, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`extbbcustaccount, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.extbbcus ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	e := Extbbcus{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&e.Extbbcustaccount, &e.EquinoxPrn, &e.EquinoxLrn, &e.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &e, nil
}
