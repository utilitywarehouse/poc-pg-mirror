// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Ghostcli represents a row from 'equinox.ghostcli'.
type Ghostcli struct {
	Ghostclinumber  sql.NullString `json:"ghostclinumber"`  // ghostclinumber
	Ghostclirealnum sql.NullString `json:"ghostclirealnum"` // ghostclirealnum
	EquinoxLrn      int64          `json:"equinox_lrn"`     // equinox_lrn
	EquinoxSec      sql.NullInt64  `json:"equinox_sec"`     // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Ghostcli exists in the database.
func (g *Ghostcli) Exists() bool {
	return g._exists
}

// Deleted provides information if the Ghostcli has been deleted from the database.
func (g *Ghostcli) Deleted() bool {
	return g._deleted
}

// Insert inserts the Ghostcli to the database.
func (g *Ghostcli) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ghostcli (` +
		`ghostclinumber, ghostclirealnum, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxSec)
	err = db.QueryRow(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxSec).Scan(&g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Update updates the Ghostcli in the database.
func (g *Ghostcli) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if g._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ghostcli SET (` +
		`ghostclinumber, ghostclirealnum, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3` +
		`) WHERE equinox_lrn = $4`

	// run query
	XOLog(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxSec, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxSec, g.EquinoxLrn)
	return err
}

// Save saves the Ghostcli to the database.
func (g *Ghostcli) Save(db XODB) error {
	if g.Exists() {
		return g.Update(db)
	}

	return g.Insert(db)
}

// Upsert performs an upsert for Ghostcli.
//
// NOTE: PostgreSQL 9.5+ only
func (g *Ghostcli) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if g._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ghostcli (` +
		`ghostclinumber, ghostclirealnum, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`ghostclinumber, ghostclirealnum, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.ghostclinumber, EXCLUDED.ghostclirealnum, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxLrn, g.EquinoxSec)
	_, err = db.Exec(sqlstr, g.Ghostclinumber, g.Ghostclirealnum, g.EquinoxLrn, g.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	g._exists = true

	return nil
}

// Delete deletes the Ghostcli from the database.
func (g *Ghostcli) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !g._exists {
		return nil
	}

	// if deleted, bail
	if g._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ghostcli WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, g.EquinoxLrn)
	_, err = db.Exec(sqlstr, g.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	g._deleted = true

	return nil
}

// GhostcliByEquinoxLrn retrieves a row from 'equinox.ghostcli' as a Ghostcli.
//
// Generated from index 'ghostcli_pkey'.
func GhostcliByEquinoxLrn(db XODB, equinoxLrn int64) (*Ghostcli, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`ghostclinumber, ghostclirealnum, equinox_lrn, equinox_sec ` +
		`FROM equinox.ghostcli ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	g := Ghostcli{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&g.Ghostclinumber, &g.Ghostclirealnum, &g.EquinoxLrn, &g.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &g, nil
}
