// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// HsSpecBlb represents a row from 'equinox.hs_spec_blb'.
type HsSpecBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the HsSpecBlb exists in the database.
func (hsb *HsSpecBlb) Exists() bool {
	return hsb._exists
}

// Deleted provides information if the HsSpecBlb has been deleted from the database.
func (hsb *HsSpecBlb) Deleted() bool {
	return hsb._deleted
}

// Insert inserts the HsSpecBlb to the database.
func (hsb *HsSpecBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if hsb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.hs_spec_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, hsb.BlbData, hsb.BlbText)
	err = db.QueryRow(sqlstr, hsb.BlbData, hsb.BlbText).Scan(&hsb.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	hsb._exists = true

	return nil
}

// Update updates the HsSpecBlb in the database.
func (hsb *HsSpecBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !hsb._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if hsb._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.hs_spec_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, hsb.BlbData, hsb.BlbText, hsb.BlbLrn)
	_, err = db.Exec(sqlstr, hsb.BlbData, hsb.BlbText, hsb.BlbLrn)
	return err
}

// Save saves the HsSpecBlb to the database.
func (hsb *HsSpecBlb) Save(db XODB) error {
	if hsb.Exists() {
		return hsb.Update(db)
	}

	return hsb.Insert(db)
}

// Upsert performs an upsert for HsSpecBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (hsb *HsSpecBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if hsb._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.hs_spec_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, hsb.BlbLrn, hsb.BlbData, hsb.BlbText)
	_, err = db.Exec(sqlstr, hsb.BlbLrn, hsb.BlbData, hsb.BlbText)
	if err != nil {
		return err
	}

	// set existence
	hsb._exists = true

	return nil
}

// Delete deletes the HsSpecBlb from the database.
func (hsb *HsSpecBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !hsb._exists {
		return nil
	}

	// if deleted, bail
	if hsb._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.hs_spec_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, hsb.BlbLrn)
	_, err = db.Exec(sqlstr, hsb.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	hsb._deleted = true

	return nil
}

// HsSpecBlbByBlbLrn retrieves a row from 'equinox.hs_spec_blb' as a HsSpecBlb.
//
// Generated from index 'hs_spec_blb_pkey'.
func HsSpecBlbByBlbLrn(db XODB, blbLrn int64) (*HsSpecBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.hs_spec_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	hsb := HsSpecBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&hsb.BlbLrn, &hsb.BlbData, &hsb.BlbText)
	if err != nil {
		return nil, err
	}

	return &hsb, nil
}
