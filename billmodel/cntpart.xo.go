// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Cntpart represents a row from 'equinox.cntpart'.
type Cntpart struct {
	Cntpindex   sql.NullInt64  `json:"cntpindex"`   // cntpindex
	Cntptype    sql.NullString `json:"cntptype"`    // cntptype
	Cntpcontent sql.NullInt64  `json:"cntpcontent"` // cntpcontent
	EquinoxPrn  sql.NullInt64  `json:"equinox_prn"` // equinox_prn
	EquinoxLrn  int64          `json:"equinox_lrn"` // equinox_lrn
	EquinoxSec  sql.NullInt64  `json:"equinox_sec"` // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Cntpart exists in the database.
func (c *Cntpart) Exists() bool {
	return c._exists
}

// Deleted provides information if the Cntpart has been deleted from the database.
func (c *Cntpart) Deleted() bool {
	return c._deleted
}

// Insert inserts the Cntpart to the database.
func (c *Cntpart) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cntpart (` +
		`cntpindex, cntptype, cntpcontent, equinox_prn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxSec)
	err = db.QueryRow(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxSec).Scan(&c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Update updates the Cntpart in the database.
func (c *Cntpart) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if c._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.cntpart SET (` +
		`cntpindex, cntptype, cntpcontent, equinox_prn, equinox_sec` +
		`) = ( ` +
		`$1, $2, $3, $4, $5` +
		`) WHERE equinox_lrn = $6`

	// run query
	XOLog(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxSec, c.EquinoxLrn)
	return err
}

// Save saves the Cntpart to the database.
func (c *Cntpart) Save(db XODB) error {
	if c.Exists() {
		return c.Update(db)
	}

	return c.Insert(db)
}

// Upsert performs an upsert for Cntpart.
//
// NOTE: PostgreSQL 9.5+ only
func (c *Cntpart) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if c._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.cntpart (` +
		`cntpindex, cntptype, cntpcontent, equinox_prn, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3, $4, $5, $6` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`cntpindex, cntptype, cntpcontent, equinox_prn, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.cntpindex, EXCLUDED.cntptype, EXCLUDED.cntpcontent, EXCLUDED.equinox_prn, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	_, err = db.Exec(sqlstr, c.Cntpindex, c.Cntptype, c.Cntpcontent, c.EquinoxPrn, c.EquinoxLrn, c.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	c._exists = true

	return nil
}

// Delete deletes the Cntpart from the database.
func (c *Cntpart) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !c._exists {
		return nil
	}

	// if deleted, bail
	if c._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.cntpart WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, c.EquinoxLrn)
	_, err = db.Exec(sqlstr, c.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	c._deleted = true

	return nil
}

// CntpartByEquinoxLrn retrieves a row from 'equinox.cntpart' as a Cntpart.
//
// Generated from index 'cntpart_pkey'.
func CntpartByEquinoxLrn(db XODB, equinoxLrn int64) (*Cntpart, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`cntpindex, cntptype, cntpcontent, equinox_prn, equinox_lrn, equinox_sec ` +
		`FROM equinox.cntpart ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	c := Cntpart{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&c.Cntpindex, &c.Cntptype, &c.Cntpcontent, &c.EquinoxPrn, &c.EquinoxLrn, &c.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &c, nil
}
