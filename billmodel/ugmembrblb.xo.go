// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// UgmembrBlb represents a row from 'equinox.ugmembr_blb'.
type UgmembrBlb struct {
	BlbLrn  int64          `json:"blb_lrn"`  // blb_lrn
	BlbData []byte         `json:"blb_data"` // blb_data
	BlbText sql.NullString `json:"blb_text"` // blb_text

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the UgmembrBlb exists in the database.
func (ub *UgmembrBlb) Exists() bool {
	return ub._exists
}

// Deleted provides information if the UgmembrBlb has been deleted from the database.
func (ub *UgmembrBlb) Deleted() bool {
	return ub._deleted
}

// Insert inserts the UgmembrBlb to the database.
func (ub *UgmembrBlb) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if ub._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ugmembr_blb (` +
		`blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING blb_lrn`

	// run query
	XOLog(sqlstr, ub.BlbData, ub.BlbText)
	err = db.QueryRow(sqlstr, ub.BlbData, ub.BlbText).Scan(&ub.BlbLrn)
	if err != nil {
		return err
	}

	// set existence
	ub._exists = true

	return nil
}

// Update updates the UgmembrBlb in the database.
func (ub *UgmembrBlb) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ub._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if ub._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.ugmembr_blb SET (` +
		`blb_data, blb_text` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE blb_lrn = $3`

	// run query
	XOLog(sqlstr, ub.BlbData, ub.BlbText, ub.BlbLrn)
	_, err = db.Exec(sqlstr, ub.BlbData, ub.BlbText, ub.BlbLrn)
	return err
}

// Save saves the UgmembrBlb to the database.
func (ub *UgmembrBlb) Save(db XODB) error {
	if ub.Exists() {
		return ub.Update(db)
	}

	return ub.Insert(db)
}

// Upsert performs an upsert for UgmembrBlb.
//
// NOTE: PostgreSQL 9.5+ only
func (ub *UgmembrBlb) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if ub._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.ugmembr_blb (` +
		`blb_lrn, blb_data, blb_text` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (blb_lrn) DO UPDATE SET (` +
		`blb_lrn, blb_data, blb_text` +
		`) = (` +
		`EXCLUDED.blb_lrn, EXCLUDED.blb_data, EXCLUDED.blb_text` +
		`)`

	// run query
	XOLog(sqlstr, ub.BlbLrn, ub.BlbData, ub.BlbText)
	_, err = db.Exec(sqlstr, ub.BlbLrn, ub.BlbData, ub.BlbText)
	if err != nil {
		return err
	}

	// set existence
	ub._exists = true

	return nil
}

// Delete deletes the UgmembrBlb from the database.
func (ub *UgmembrBlb) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !ub._exists {
		return nil
	}

	// if deleted, bail
	if ub._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.ugmembr_blb WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, ub.BlbLrn)
	_, err = db.Exec(sqlstr, ub.BlbLrn)
	if err != nil {
		return err
	}

	// set deleted
	ub._deleted = true

	return nil
}

// UgmembrBlbByBlbLrn retrieves a row from 'equinox.ugmembr_blb' as a UgmembrBlb.
//
// Generated from index 'ugmembr_blb_pkey'.
func UgmembrBlbByBlbLrn(db XODB, blbLrn int64) (*UgmembrBlb, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`blb_lrn, blb_data, blb_text ` +
		`FROM equinox.ugmembr_blb ` +
		`WHERE blb_lrn = $1`

	// run query
	XOLog(sqlstr, blbLrn)
	ub := UgmembrBlb{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, blbLrn).Scan(&ub.BlbLrn, &ub.BlbData, &ub.BlbText)
	if err != nil {
		return nil, err
	}

	return &ub, nil
}
