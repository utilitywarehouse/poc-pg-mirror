// Package billmodel contains the types for schema 'equinox'.
package billmodel

// GENERATED BY XO. DO NOT EDIT.

import (
	"database/sql"
	"errors"
)

// Sharopt represents a row from 'equinox.sharopts'.
type Sharopt struct {
	Soexid     sql.NullString `json:"soexid"`      // soexid
	EquinoxLrn int64          `json:"equinox_lrn"` // equinox_lrn
	EquinoxSec sql.NullInt64  `json:"equinox_sec"` // equinox_sec

	// xo fields
	_exists, _deleted bool
}

// Exists determines if the Sharopt exists in the database.
func (s *Sharopt) Exists() bool {
	return s._exists
}

// Deleted provides information if the Sharopt has been deleted from the database.
func (s *Sharopt) Deleted() bool {
	return s._deleted
}

// Insert inserts the Sharopt to the database.
func (s *Sharopt) Insert(db XODB) error {
	var err error

	// if already exist, bail
	if s._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.sharopts (` +
		`soexid, equinox_sec` +
		`) VALUES (` +
		`$1, $2` +
		`) RETURNING equinox_lrn`

	// run query
	XOLog(sqlstr, s.Soexid, s.EquinoxSec)
	err = db.QueryRow(sqlstr, s.Soexid, s.EquinoxSec).Scan(&s.EquinoxLrn)
	if err != nil {
		return err
	}

	// set existence
	s._exists = true

	return nil
}

// Update updates the Sharopt in the database.
func (s *Sharopt) Update(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !s._exists {
		return errors.New("update failed: does not exist")
	}

	// if deleted, bail
	if s._deleted {
		return errors.New("update failed: marked for deletion")
	}

	// sql query
	const sqlstr = `UPDATE equinox.sharopts SET (` +
		`soexid, equinox_sec` +
		`) = ( ` +
		`$1, $2` +
		`) WHERE equinox_lrn = $3`

	// run query
	XOLog(sqlstr, s.Soexid, s.EquinoxSec, s.EquinoxLrn)
	_, err = db.Exec(sqlstr, s.Soexid, s.EquinoxSec, s.EquinoxLrn)
	return err
}

// Save saves the Sharopt to the database.
func (s *Sharopt) Save(db XODB) error {
	if s.Exists() {
		return s.Update(db)
	}

	return s.Insert(db)
}

// Upsert performs an upsert for Sharopt.
//
// NOTE: PostgreSQL 9.5+ only
func (s *Sharopt) Upsert(db XODB) error {
	var err error

	// if already exist, bail
	if s._exists {
		return errors.New("insert failed: already exists")
	}

	// sql query
	const sqlstr = `INSERT INTO equinox.sharopts (` +
		`soexid, equinox_lrn, equinox_sec` +
		`) VALUES (` +
		`$1, $2, $3` +
		`) ON CONFLICT (equinox_lrn) DO UPDATE SET (` +
		`soexid, equinox_lrn, equinox_sec` +
		`) = (` +
		`EXCLUDED.soexid, EXCLUDED.equinox_lrn, EXCLUDED.equinox_sec` +
		`)`

	// run query
	XOLog(sqlstr, s.Soexid, s.EquinoxLrn, s.EquinoxSec)
	_, err = db.Exec(sqlstr, s.Soexid, s.EquinoxLrn, s.EquinoxSec)
	if err != nil {
		return err
	}

	// set existence
	s._exists = true

	return nil
}

// Delete deletes the Sharopt from the database.
func (s *Sharopt) Delete(db XODB) error {
	var err error

	// if doesn't exist, bail
	if !s._exists {
		return nil
	}

	// if deleted, bail
	if s._deleted {
		return nil
	}

	// sql query
	const sqlstr = `DELETE FROM equinox.sharopts WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, s.EquinoxLrn)
	_, err = db.Exec(sqlstr, s.EquinoxLrn)
	if err != nil {
		return err
	}

	// set deleted
	s._deleted = true

	return nil
}

// SharoptByEquinoxLrn retrieves a row from 'equinox.sharopts' as a Sharopt.
//
// Generated from index 'sharopts_pkey'.
func SharoptByEquinoxLrn(db XODB, equinoxLrn int64) (*Sharopt, error) {
	var err error

	// sql query
	const sqlstr = `SELECT ` +
		`soexid, equinox_lrn, equinox_sec ` +
		`FROM equinox.sharopts ` +
		`WHERE equinox_lrn = $1`

	// run query
	XOLog(sqlstr, equinoxLrn)
	s := Sharopt{
		_exists: true,
	}

	err = db.QueryRow(sqlstr, equinoxLrn).Scan(&s.Soexid, &s.EquinoxLrn, &s.EquinoxSec)
	if err != nil {
		return nil, err
	}

	return &s, nil
}
